import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        flexDirection: 'row',
        padding: 10,
    },

    item: {
        padding: 10,
    },

    thing: {
        // borderWidth: 1,
        // borderColor: 'white',
        flex: 1, 
    }
})